//* Selectors can save, update or convert the data in the final shape that you need
//* You can have multiple selectors with diferent transformations of the data
//* into the final desired output

export const selectCurrentUser = (state) => state.user.currentUser 